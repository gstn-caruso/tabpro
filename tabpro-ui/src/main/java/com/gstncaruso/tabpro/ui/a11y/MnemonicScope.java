package com.gstncaruso.tabpro.ui.a11y;

/**
 * Marca un contenedor (tipicamente un formulario) como su propio ambito de mnemonicos: las
 * etiquetas de dos {@link MnemonicScope} distintos pueden compartir letra sin que sea un
 * choque, porque cada uno la asigna sin saber del otro.
 */
public interface MnemonicScope {
}
