package com.gstncaruso.tabpro.core.notation;

/** La alteracion que hay que dibujar al lado de una nota, si es que hace falta alguna. */
public enum AccidentalGlyph {
    NONE,
    SHARP,
    FLAT,
    NATURAL
}
