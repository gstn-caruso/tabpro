<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.gstncaruso.tabpro</groupId>
  <artifactId>tabpro-parent</artifactId>
  <version>0.59.5</version>
  <packaging>pom</packaging>

  <name>Tabpro</name>
  <description>Free Guitar Pro 5 clone: tablature editor for guitar.</description>
  <url>https://github.com/gstn-caruso/tabpro</url>

  <licenses>
    <license>
      <name>MIT License</name>
      <url>https://opensource.org/licenses/MIT</url>
    </license>
  </licenses>

  <modules>
    <module>tabpro-core</module>
    <module>tabpro-midi</module>
    <module>tabpro-format</module>
    <module>tabpro-ui</module>
    <module>tabpro-app</module>
    <module>tabpro-tests</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>25</maven.compiler.release>
    <junit.version>5.11.4</junit.version>
    <tests.excluded.groups>integration</tests.excluded.groups>
    <!--
      Real-use audit tests (tabpro-app/src/test/.../audit) instantiate the main window to dispatch real keyboard and mouse events:
      they need a non-headless toolkit for focus and true peers. Defaults to true (same as mvn -B verify has always run);
      only the integration group is invoked with -Dtests.headless=false.
    -->
    <tests.headless>true</tests.headless>
    <!--
      Locale-dependent tests must pass under both es_ES (dev machine) and en_US (GitHub runner).
      Override with -Dtests.locale="-Duser.language=es -Duser.country=ES" to run tests in Spanish locale.
    -->
    <tests.locale></tests.locale>
    <flatlaf.version>3.7.2</flatlaf.version>
    <gson.version>2.14.0</gson.version>
    <jsvg.version>2.1.0</jsvg.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.formdev</groupId>
        <artifactId>flatlaf</artifactId>
        <version>${flatlaf.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.code.gson</groupId>
        <artifactId>gson</artifactId>
        <version>${gson.version}</version>
      </dependency>
      <dependency>
        <groupId>com.github.weisj</groupId>
        <artifactId>jsvg</artifactId>
        <version>${jsvg.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.2</version>
          <configuration>
            <!--
              add-exports is for WaveRenderer (tabpro-midi): it renders WAVE off real-time using com.sun.media.sound.AudioSynthesizer,
              the only path exposed by Gervill (the JDK synthesizer). Not public API, but the only way to get audio from the synthesizer
              faster than real-time.
            -->
            <!--
              TieredStopAtLevel=1 keeps JIT at C1. The entire suite runs in half a second: compiling with C2 costs more than
              it can return before the VM terminates.
            -->
            <!--
              java.util.prefs.userRoot and java.io.tmpdir are isolated: MainFrame saves Preferences (recent files, autosave, etc.)
              in java.util.prefs.Preferences.userRoot(), and ScoreDocument looks for its recovery file in java.io.tmpdir +
              "tabpro-recuperacion" — the same paths a real installed tabpro uses on this machine. Real-use audit tests (tabpro-app/.../audit)
              instantiate MainFrame: without this, a real recovery file left from a real session would trigger a real dialog (offerToRecover)
              in each test, and preferences read or written would be the real ones from whoever runs the suite.
            -->
            <argLine>-Djava.awt.headless=${tests.headless} -XX:TieredStopAtLevel=1 --add-exports=java.desktop/com.sun.media.sound=ALL-UNNAMED -Djava.util.prefs.userRoot=${project.build.directory}/test-prefs-root -Djava.io.tmpdir=${project.build.directory} ${tests.locale}</argLine>
            <excludedGroups>${tests.excluded.groups}</excludedGroups>
            <skipTests>true</skipTests>
            <properties>
              <configurationParameters>
                junit.jupiter.execution.parallel.enabled=true
                junit.jupiter.execution.parallel.mode.default=concurrent
                junit.jupiter.execution.parallel.mode.classes.default=concurrent
              </configurationParameters>
            </properties>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
